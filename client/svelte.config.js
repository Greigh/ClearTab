/* Minimal Svelte 5 config. Extend here as needed (e.g.,
   compilerOptions, preprocess pipelines).
   CJS syntax because the project's package.json has
   "type": "commonjs". The plugin handles either. */
module.exports = {};
