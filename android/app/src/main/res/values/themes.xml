<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window chrome only; Compose drives all in-app colors. windowBackground
         doubles as the cold-start launch screen on API 26–30 (API 31+ uses the
         system splash in values-v31/themes.xml). -->
    <style name="Theme.FiHaven" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>
