<?xml version="1.0" encoding="utf-8"?>
<!-- Debug only: allow cleartext to the local Express dev server
     (emulator host alias 10.0.2.2, plus localhost) while keeping
     everything else HTTPS-only. Overrides the release config of the
     same name via the debug source set. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
